# v0.2.0 — Repository Operating System

**Released:** 2026-05-17

## Highlights
- Full repository operating system: root docs (README, ARCHITECTURE, DEPLOYMENT, TESTING, SECURITY, OPERATIONS, BUSINESS_CONTEXT, MARKETING_GROWTH, BENCHMARK, ROADMAP, PROJECT_STATUS, DECISIONS, TODO, VERSION, CHANGELOG) expanded from short stubs into production-grade documentation.
- `SKILLS/` playbooks rewritten and three new playbooks added (`ai.md`, `database.md`, `integrations.md`).
- New `docs/` tree (wiki, api, sdk, runbooks, compliance, design-system, product, business, growth, security, testing, deployment).
- API and SDK planning documentation (`docs/api/`, `docs/sdk/`).
- CI workflow fixed to target `UI-WEB/frontend`; added a non-blocking `API-Node` install + audit job.

## What's not in this release
- Code refactors. No production code changed; this is a docs + CI release.
- No removed pages, components, or routes.

## Upgrade Notes
See `VERSION.md` "Upgrade Notes (v0.1.0 → v0.2.0)".
