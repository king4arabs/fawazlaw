# Benchmark

## Current Baseline
- Frontend build has pre-existing CRA warnings that fail when `CI=true`.
- Dependency audit reports high and critical vulnerabilities.
- Static SEO pages, sitemap, manifest, and robots are now present.

## Target
- Successful CI build/test.
- Lighthouse mobile performance and accessibility improvements.
- No high/critical production dependency vulnerabilities.
